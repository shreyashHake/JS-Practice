console.log(`
>> Challenge 2 : `)

if (BMI_Mark > BMI_John) {
    console.log("Mark's BMI is higher than John's!")
} else {
    console.log("Mark's BMI is smaller than John's!")
}


if (BMI_Mark > BMI_John) {
    console.log(`Mark's BMI(${BMI_Mark}) is higher than John's(${BMI_John}) !`)
} else {
    console.log(`Mark's BMI(${BMI_Mark}) is smaller than John's  (${BMI_John})!`)
}
